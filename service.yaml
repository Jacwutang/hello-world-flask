apiVersion: v1
kind: Service
metadata:
  name: hello-world-service
spec:
  selector:
    app: hello-world
  ports:
    - protocol: TCP
      port: 5001
      targetPort: 5001
  type: ClusterIP  # or LoadBalancer/NodePort depending on your use case